# as importações como em outras linguagens é dessa forma
# library(pacote) → se não achar, dá erro.
# require(pacote) → se não achar, retorna FALSE e segue o código.
library(R6)
# para instalar no vs code eu preciso de rodar
# install.packages("R6")